language: none

services:
  - cassandra 
  - elasticsearch 
  - memcached 
##  - mongodb 
 # - mysql 
 # - neo4j 
##  - postgres 
 # - rabbitmq 
  - redis  
 # - selenium
 # - sqlite 
 # - couchdb 
 # - rethinkdb 
  - riak 
  
build:
  ci:
    - node --version
    - cassandra -v
    - elasticsearch -v
    - memcached -v
    - mongod -v
    - psql --version
    - redis-server -v
    - riak version
    
  pre_ci_boot:
    image_name: drydock/u14all
    image_tag: master
    pull: true  
